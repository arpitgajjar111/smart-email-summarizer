# Smart Email Summarizer — Python Dependencies
# Install with: pip install -r requirements.txt

# ── Web Framework ──────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1

# ── Data Validation ───────────────────────
pydantic==2.7.4

# ── AI / NLP ──────────────────────────────
transformers==4.41.2
torch==2.3.1             # CPU build; for GPU: pip install torch --index-url https://download.pytorch.org/whl/cu121
sentencepiece==0.2.0     # Required by PEGASUS and T5 tokenizers
accelerate==0.30.0       # Faster model loading

# ── File Extraction ───────────────────────
pymupdf==1.24.3          # PDF extraction (fitz)
python-docx==1.1.2       # DOCX extraction
pdfplumber==0.11.1       # Fallback PDF extractor

# ── Utilities ─────────────────────────────
python-multipart==0.0.9  # Required by FastAPI for file uploads
aiofiles==23.2.1         # Async file serving

# ── Development ───────────────────────────
httpx==0.27.0            # For testing API endpoints
pytest==8.2.2
pytest-asyncio==0.23.7
